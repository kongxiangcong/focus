import type { VariantProps } from "../variants";
import { ReaderExperience } from "../ReaderExperience";
export function Duet({ host }: VariantProps) { return <ReaderExperience host={host} direction="duet" />; }
