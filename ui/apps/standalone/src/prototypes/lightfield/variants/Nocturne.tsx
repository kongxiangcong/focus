import type { VariantProps } from "../variants";
import { ReaderExperience } from "../ReaderExperience";
export function Nocturne({ host }: VariantProps) { return <ReaderExperience host={host} direction="nocturne" />; }
