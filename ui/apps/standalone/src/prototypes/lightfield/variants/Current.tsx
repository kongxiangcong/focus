import type { VariantProps } from "../variants";
import { ReaderExperience } from "../ReaderExperience";
export function Current({ host }: VariantProps) { return <ReaderExperience host={host} direction="current" />; }
