import type { VariantProps } from "../variants";
import { ReaderExperience } from "../ReaderExperience";
export function Folio({ host }: VariantProps) { return <ReaderExperience host={host} direction="folio" />; }
