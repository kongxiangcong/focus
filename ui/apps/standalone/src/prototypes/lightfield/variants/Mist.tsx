import type { VariantProps } from "../variants";
import { ReaderExperience } from "../ReaderExperience";
export function Mist({ host }: VariantProps) { return <ReaderExperience host={host} direction="mist" />; }
