# 后台进度反馈 — 2026-09-17

需求：对话或上传过程中可看见后台进展，避免回复结束但仍在处理时没有反馈。

Host 通过已有 SSE ReadingWindow 投影当前阶段与 startedAt/updatedAt/finishedAt；进度不进入 Core 阅读状态。阶段取自连接、回复流、工具、解析命令、规划、保存和校验事件。只显示活动类别，不展示推理内容、命令或原始输出。不估算百分比与剩余时间。

阅读输入框上方与知识库任务条共用 TaskProgress。显示阶段、已用时间、不定量进度线；15 秒没有新事件时显示“暂未收到新进展”，批准等待与停止单独显示。完成后计时冻结，后台重启中断也记录结束时间。旧 Host 无时间戳时只投影已有 activity，不制造耗时。

修复原来收到模型 turn/completed 即发布完成的问题：成功状态须在 Host 最终验证结束后才能发布；验证失败直接显示失败。

验证：前端 30 项通过，类型检查和生产构建通过；新增后台进度 4 项通过。全套 Python 在最后新增一个适配器映射测试前为 119 项、118 通过，唯一失败仍为既有五技能数量断言；新增测试已单独通过。构建保留既有 >500 kB 提示。

独立样例 Host 浏览器验收：1280×720 阅读状态栏、390×844 知识库状态条，含静默提示和计时；窄屏文档宽度 390，无横向溢出。样例未调用真实模型/MinerU，不能据此声称验证过真实远端解析进度。MinerU 没有通过当前事件接口提供总页数/阶段百分比。

前端 dist 已构建；新后端阶段/时间字段需重启 Host 后启用。此前停止/重启现有服务被自动审批拒绝，本次未绕过限制。未提交或推送，保留上一轮修改。
