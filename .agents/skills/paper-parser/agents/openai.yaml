interface:
  display_name: "Paper Parser"
  short_description: "Parse papers with MinerU precision API"
  default_prompt: "Use $paper-parser to parse and register this selected PDF in my private FOCUS Source Library now; this invocation authorizes the required MinerU upload."
